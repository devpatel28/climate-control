# Pass as shell environment variables. This file is a reference, not auto-loaded.
MODE=simulate
HOST=127.0.0.1
PORT=8100
# For hardware mode set MODE=serial and SERIAL_PATH to COM3 or /dev/ttyACM0.
SERIAL_PATH=
